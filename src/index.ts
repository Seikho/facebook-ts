import accessToken = require('./api/accessToken');
import getUser = require('./api/getUser');
import settings = require('./api/settings');
import verifyToken = require('./api/verifyToken');

export = {
    settings,
    getUser,
    accessToken,
    verifyToken
}